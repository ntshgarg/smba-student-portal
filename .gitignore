/node_modules
/.next
/out
/coverage
/.data
*.log
*.tsbuildinfo
.DS_Store
.env*
!.env.example

# Local audit, browser-test, and visual-regression artifacts
/output
/snapshots
/tmp
/test-results
/playwright-report
/static_analysis_codeql_1
/static_analysis_semgrep_1
/audit-context
/.supply-chain-risk-auditor
.vercel

# Agent worktrees and local settings live here; never commit them.
/.claude

# End-to-end flow harnesses write live owner and head-coach credentials, PINs
# and TOTP seeds into flowtest/state.json; never commit them.
/flowtest
